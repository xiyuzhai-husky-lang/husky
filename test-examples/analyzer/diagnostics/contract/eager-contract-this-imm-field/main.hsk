dataset:
    synthetic::trivial::real1d::dataset1()

struct B:
    x: i32

func f(b: &B) -> i32:
    1

func g(a: &A) -> i32:
    c = f(a.b)
    1

func h(v: &[]A) -> i32:
    t1 = g(v.first())
    t2 = v.first()
    1

proc i(v: &[]A) -> i32:
    var t1 = g(v.first())
    var t2 = v.first()
    return 1

struct A:
    b: B

    func g -> i32:
        c = f(b)
        1

main:
    a = A(B(1))
    c = f(a.b)
    1