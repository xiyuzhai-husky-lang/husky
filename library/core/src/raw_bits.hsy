use crate::*

#[rust("u32")]
#derive(Debug, PartialEq, Eq, Clone, Copy)
pub extern r32;

impl r32:
    pub fn last_bits(k: i32) -> r32;

    // count trailing zeros
    pub fn ctz() -> i32;

    pub fn co() -> i32;

    pub fn span() -> i32;

    pub fn right_mass() -> i32;