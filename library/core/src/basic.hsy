use crate::*

#derive(Debug, PartialEq, Eq, Clone, Copy)
pub extern bool;

#derive(Debug, PartialEq, Eq, Clone, Copy)
pub extern never;

#derive(Debug, PartialEq, Eq, Clone, Copy)
pub extern unit;

// todo: move this to another module
pub extern Trait;

// todo: move this to another module
pub extern Module;

// todo: move this to another module
pub extern Lifetime;

// todo: move this to another module
pub extern Place;