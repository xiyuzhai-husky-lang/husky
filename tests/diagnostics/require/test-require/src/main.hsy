task:
    ml::datasets::synthetic::trivial::real1d::dataset1()

func try_require_in_func() -> ?i32:
    require 2
    1

main:
    // b = B(3, A(1))
    // c = use_eval_ref_copyable_is_ok(b.x)
    // x = 1
    // y = take_copyable_eval_ref(x)
    1