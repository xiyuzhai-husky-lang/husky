[package]
name = "dev-utils"
version = "0.0.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
serde = { version = "1.0.106", features = ["derive"] }
serde_json = { version = "1.0.48", features = ["preserve_order"] }
