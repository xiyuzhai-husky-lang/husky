[package]
name = "trivial-iter"
version = "0.0.0"
description = "Trivial iterator for the Husky Programming Language"
license = "MIT OR Apache-2.0"
edition = "2021"

[lib]
doctest = false

[dependencies]

[dev-dependencies]
check-utils = { path = "../../utils/check" }
print-utils = { path = "../../utils/print" }
