[package]
name = "husky-toml-token-sheet"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[dependencies]
husky-toml-token = { path = "../../lex/husky-toml-token", features = [
    "storage"
] }
