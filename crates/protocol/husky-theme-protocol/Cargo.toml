[package]
name = "husky-theme-protocol"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[dependencies]
husky-token-protocol = { path = "../../protocol/husky-token-protocol" }
