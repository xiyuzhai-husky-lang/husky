use super::*;

impl<'db> GeminiClient<'db> {
    pub fn generate_on_paid(
        &self,
        model: GeminiModel,
        request: GeminiRequest,
    ) -> GeminiResult<GeminiResponse> {
        todo!()
    }
}
