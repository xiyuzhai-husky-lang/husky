[package]
name = "husky-vfs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[dependencies]
salsa = "0.17.0-pre.2"
notify-debouncer-mini = "0.2.0"
timed-salsa = { path = "../../abstraction/timed-salsa" }
