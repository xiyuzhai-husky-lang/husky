[package]
edition = "2021"
name = "infer-total"
version = "0.0.0"
authors = ["Xiyu Zhai"]
license = "MIT OR Apache-2.0"
description = "Generic LSP server scaffold."

[dependencies]
infer-error = { path = "../error", version = "0.0.0" }
infer-signature = { path = "../signature", version = "0.0.0" }
infer-ty = { path = "../ty", version = "0.0.0" }
infer-contract = { path = "../contract", version = "0.0.0" }
