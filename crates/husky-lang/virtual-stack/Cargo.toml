[package]
name = "virtual-stack"
version = "0.0.0"
edition = "2021"

[dependencies]
common = { path = "../../common", version = "0.0.0" }
syntax-types = { path = "../syntax/types", version = "0.0.0" }
