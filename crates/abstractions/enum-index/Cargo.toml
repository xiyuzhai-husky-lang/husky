[package]
name = "enum-index"
version.workspace = true
edition = "2021"

[dependencies]
serde = { workspace = true, optional = true }
# abstractions
enum-index-macros.workspace = true

[features]
serde = ["dep:serde"]
