[package]
name = "enum-index-macros"
version.workspace = true
edition = "2021"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { workspace = true, features = ["full", "extra-traits", "visit-mut"] }
