[package]
name = "husky-case-utils"
version.workspace = true
edition = "2021"

[dependencies]
convert_case.workspace = true
relative-path = { workspace = true, optional = true }

[features]
relative_path = ["dep:relative-path"]
