use super::*;

#[derive(Debug, PartialEq, Eq, Clone, Serialize, Deserialize)]
pub struct GenericGraphics2dFigure;

impl GenericGraphics2dFigure {
    pub(crate) fn new() -> Self {
        GenericGraphics2dFigure
    }
}
