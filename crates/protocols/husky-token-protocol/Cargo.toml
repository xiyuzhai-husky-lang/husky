[package]
name = "husky-token-protocol"
version.workspace = true
edition = "2021"

[dependencies]
serde = { workspace = true, optional = true }
# abstractions
enum-index = { path = "../../abstractions/enum-index" }
# protocols
husky-entity-protocol = { path = "../../protocols/husky-entity-protocol" }

[features]
serde = ["dep:serde"]
