[package]
name = "vc"
version = "0.0.0"
description = "Issuer for the Husky Programming Language"
license = "MIT OR Apache-2.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
check-utils = { path = "../../utils/check", version = "0.0.0" }
print-utils = { path = "../../utils/print", version = "0.0.0" }
sync-utils = { path = "../../utils/sync", version = "0.0.0" }
# stdx = { path = "../../stdx", version = "0.0.0" }
# common = { path = "../../common", version = "0.0.0" }
