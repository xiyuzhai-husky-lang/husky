[package]
name = "husky-toml-ast"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[dependencies]
toml_datetime = "0.5.0"
thiserror = "1.0.37"
ordered-float = "3.0.0"
# abstraction
salsa = { path = "../../abstraction/salsa" }
idx-arena = { path = "../../abstraction/idx-arena" }
# fs
husky-source-path = { path = "../../fs/husky-source-path" }
husky-vfs = { path = "../../fs/husky-vfs" }
# kernel
husky-word = { path = "../../kernel/husky-word" }
# lex
husky-toml-token = { path = "../../lex/husky-toml-token" }
husky-toml-tokenize = { path = "../../lex/husky-toml-tokenize" }
husky-toml-token-stream = { path = "../../lex/husky-toml-token-stream" }
