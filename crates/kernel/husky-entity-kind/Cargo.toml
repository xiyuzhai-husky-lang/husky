[package]
edition = "2021"
name = "husky-entity-kind"
version = "0.0.0"
authors = ["Xiyu Zhai"]
license = "MIT OR Apache-2.0"
description = "Generic LSP server scaffold."

[dependencies]
serde.workspace = true
enum-class = { path = "../../abstractions/enum-class" }
husky-entity-protocol = { path = "../../protocols/husky-entity-protocol", optional = true }

[features]
protocol_support = ["dep:husky-entity-protocol"]
