[package]
edition = "2021"
name = "husky-item-taxonomy"
version = "0.0.0"
authors = ["Xiyu Zhai"]
license = "MIT OR Apache-2.0"
description = "Generic LSP server scaffold."

[dependencies]
serde = { version = "1.0.106", features = ["derive"] }
enum-class = { path = "../../abstraction/enum-class" }
