```rust
Some(
    [
        "ct",
        "ct.contour_len",
        "let len = ct.contour_len",
        "len",
        "4.0",
        "len > 4.0",
        "len > 4.0",
        "require len > 4.0",
        "len",
        "0.0",
        "len + 0.0",
        "len + 0.0",
        "let len = ct.contour_len\n    require len > 4.0\n    // ad hoc\n    len + 0.0",
    ],
)
```