## `one_fermi_match` decl

```rust
Some(
    [
        "FermiMatchResult",
    ],
)
```

## `one_fermi_match` defn

```rust
Some(
    [
        "fermi_match",
        "major_concave_components",
        "downmost",
        "upmost",
        "hat",
        "[downmost, upmost, hat]",
        "fermi_match(major_concave_components, [downmost, upmost, hat])",
        "fermi_match(major_concave_components, [downmost, upmost, hat])",
        "fermi_match(major_concave_components, [downmost, upmost, hat])",
    ],
)
```

## `is_one` decl

```rust
Some(
    [
        "OneVsAll",
        "MnistLabel",
        "OneVsAll MnistLabel",
        "MnistLabel::One",
        "OneVsAll MnistLabel MnistLabel::One",
    ],
)
```

## `is_one` defn

```rust
Some(
    [
        "narrow_down",
        "major_connected_component",
        "major_connected_component.max_hole_ilen",
        "ignored_connected_components_row_span_sum_sum",
        "5",
        "narrow_down(\n        major_connected_component.max_hole_ilen,\n        ignored_connected_components_row_span_sum_sum,\n        skip = 5,\n    )",
        "narrow_down(\n        major_connected_component.max_hole_ilen,\n        ignored_connected_components_row_span_sum_sum,\n        skip = 5,\n    )?",
        "narrow_down(\n        major_connected_component.max_hole_ilen,\n        ignored_connected_components_row_span_sum_sum,\n        skip = 5,\n    )?",
        "fermi_match",
        "major_concave_components",
        "[]",
        "fermi_match(major_concave_components, [])",
        "let simp_one_match = fermi_match(major_concave_components, [])",
        "simp_one_match",
        "simp_one_match.norm",
        "3.0",
        "simp_one_match.norm < 3.0",
        "simp_one_match.norm < 3.0",
        "narrow_down",
        "major_connected_component",
        "major_connected_component.max_row_span",
        "5",
        "narrow_down(\n            major_connected_component.max_row_span,\n            skip = 5,\n        )",
        "narrow_down(\n            major_connected_component.max_row_span,\n            skip = 5,\n        )?",
        "narrow_down(\n            major_connected_component.max_row_span,\n            skip = 5,\n        )?",
        "major_connected_component",
        "major_connected_component.max_row_span",
        "6.5",
        "major_connected_component.max_row_span > 6.5",
        "major_connected_component.max_row_span > 6.5",
        "major_connected_component",
        "major_connected_component.max_hole_ilen",
        "0.0",
        "major_connected_component.max_hole_ilen == 0.0",
        "major_connected_component.max_hole_ilen == 0.0",
        "require major_connected_component.max_hole_ilen == 0.0",
        "if major_connected_component.max_row_span > 6.5:\n            require major_connected_component.max_hole_ilen == 0.0",
        "OneVsAll::Yes",
        "OneVsAll::Yes",
        "major_connected_component",
        "major_connected_component.max_hole_ilen",
        "0.0",
        "major_connected_component.max_hole_ilen == 0.0",
        "major_connected_component.max_hole_ilen == 0.0",
        "require major_connected_component.max_hole_ilen == 0.0",
        "ignored_connected_components_row_span_sum_sum",
        "0.0",
        "ignored_connected_components_row_span_sum_sum == 0.0",
        "ignored_connected_components_row_span_sum_sum == 0.0",
        "require ignored_connected_components_row_span_sum_sum == 0.0",
        "one_fermi_match",
        "one_fermi_match.matches",
        "0",
        "one_fermi_match.matches[0]",
        "let downmost = one_fermi_match.matches[0]",
        "one_fermi_match",
        "one_fermi_match.matches",
        "1",
        "one_fermi_match.matches[1]",
        "let upmost = one_fermi_match.matches[1]",
        "one_fermi_match",
        "one_fermi_match.matches",
        "2",
        "one_fermi_match.matches[2]",
        "let hat = one_fermi_match.matches[2]",
        "downmost",
        "downmost be None",
        "simp_one_match",
        "simp_one_match.norm",
        "4.2",
        "simp_one_match.norm < 4.2",
        "simp_one_match.norm < 4.2",
        "require simp_one_match.norm < 4.2",
        "narrow_down",
        "simp_one_match",
        "simp_one_match.angle_change_norm",
        "simp_one_match.angle_change_norm.abs()",
        "5",
        "narrow_down(\n                simp_one_match.angle_change_norm.abs(),\n                skip = 5,\n            )",
        "narrow_down(\n                simp_one_match.angle_change_norm.abs(),\n                skip = 5,\n            )?",
        "narrow_down(\n                simp_one_match.angle_change_norm.abs(),\n                skip = 5,\n            )?",
        "2.0",
        "major_connected_component",
        "major_connected_component.lower_mass",
        "2.0 * major_connected_component.lower_mass",
        "major_connected_component",
        "major_connected_component.upper_mass",
        "2.0 * major_connected_component.lower_mass - major_connected_component.upper_mass",
        "52.0",
        "2.0 * major_connected_component.lower_mass - major_connected_component.upper_mass < 52.0",
        "2.0 * major_connected_component.lower_mass - major_connected_component.upper_mass < 52.0",
        "require 2.0 * major_connected_component.lower_mass - major_connected_component.upper_mass < 52.0",
        "OneVsAll::Yes",
        "return OneVsAll::Yes",
        "if downmost be None:\n            require simp_one_match.norm < 4.2\n            narrow_down(\n                simp_one_match.angle_change_norm.abs(),\n                skip = 5,\n            )?\n            require 2.0 * major_connected_component.lower_mass - major_connected_component.upper_mass < 52.0\n            return OneVsAll::Yes",
        "downmost",
        "downmost!",
        "downmost!.strokes",
        "downmost!.strokes.end()",
        "downmost",
        "downmost!",
        "downmost!.strokes",
        "downmost!.strokes.start()",
        "downmost!.strokes.end() - downmost!.strokes.start()",
        "let downmost_number_of_strokes = downmost!.strokes.end() - downmost!.strokes.start()",
        "one_fermi_match",
        "one_fermi_match.norm",
        "1.0",
        "one_fermi_match.norm < 1.0",
        "one_fermi_match.norm < 1.0",
        "require one_fermi_match.norm < 1.0",
        "narrow_down",
        "major_connected_component",
        "3",
        "major_connected_component.top_k_row_span_sum(3)",
        "one_fermi_match",
        "one_fermi_match.norm",
        "one_fermi_match",
        "one_fermi_match.rel_norm",
        "one_fermi_match",
        "one_fermi_match.angle_change_norm",
        "12",
        "narrow_down(\n            major_connected_component.top_k_row_span_sum(3),\n            one_fermi_match.norm,\n            one_fermi_match.rel_norm,\n            one_fermi_match.angle_change_norm,\n            skip = 12,\n        )",
        "narrow_down(\n            major_connected_component.top_k_row_span_sum(3),\n            one_fermi_match.norm,\n            one_fermi_match.rel_norm,\n            one_fermi_match.angle_change_norm,\n            skip = 12,\n        )?",
        "narrow_down(\n            major_connected_component.top_k_row_span_sum(3),\n            one_fermi_match.norm,\n            one_fermi_match.rel_norm,\n            one_fermi_match.angle_change_norm,\n            skip = 12,\n        )?",
        "narrow_down",
        "one_fermi_match",
        "one_fermi_match.matches",
        "0",
        "one_fermi_match.matches[0]",
        "one_fermi_match.matches[0]!",
        "one_fermi_match.matches[0]!.rel_norm",
        "one_fermi_match",
        "one_fermi_match.matches",
        "0",
        "one_fermi_match.matches[0]",
        "one_fermi_match.matches[0]!",
        "one_fermi_match.matches[0]!.angle_change",
        "one_fermi_match.matches[0]!.angle_change.abs()",
        "2",
        "narrow_down(\n            one_fermi_match.matches[0]!.rel_norm,\n            one_fermi_match.matches[0]!.angle_change.abs(),\n            skip = 2,\n        )",
        "narrow_down(\n            one_fermi_match.matches[0]!.rel_norm,\n            one_fermi_match.matches[0]!.angle_change.abs(),\n            skip = 2,\n        )?",
        "narrow_down(\n            one_fermi_match.matches[0]!.rel_norm,\n            one_fermi_match.matches[0]!.angle_change.abs(),\n            skip = 2,\n        )?",
        "upmost",
        "upmost be Some(_)",
        "upmost",
        "upmost!",
        "upmost!.strokes",
        "upmost!.strokes.end()",
        "upmost",
        "upmost!",
        "upmost!.strokes",
        "upmost!.strokes.start()",
        "upmost!.strokes.end() - upmost!.strokes.start()",
        "2",
        "upmost!.strokes.end() - upmost!.strokes.start() == 2",
        "upmost!.strokes.end() - upmost!.strokes.start() == 2",
        "require upmost!.strokes.end() - upmost!.strokes.start() == 2",
        "upmost",
        "upmost!",
        "upmost!.strokes",
        "upmost",
        "upmost!",
        "upmost!.strokes",
        "upmost!.strokes.start()",
        "1",
        "upmost!.strokes.start() + 1",
        "upmost!.strokes[upmost!.strokes.start() + 1]",
        "let long_vertical = upmost!.strokes[upmost!.strokes.start() + 1]",
        "long_vertical",
        "long_vertical.displacement()",
        "let long_vertical_dp = long_vertical.displacement()",
        "long_vertical_dp",
        "long_vertical_dp.y",
        "0.0",
        "long_vertical_dp.y > 0.0",
        "long_vertical_dp.y > 0.0",
        "require long_vertical_dp.y > 0.0",
        "narrow_down",
        "upmost",
        "upmost!",
        "upmost!.norm",
        "upmost",
        "upmost!",
        "upmost!.rel_norm",
        "upmost",
        "upmost!",
        "upmost!.angle_change",
        "upmost!.angle_change.abs()",
        "2",
        "narrow_down(\n                upmost!.norm,\n                upmost!.rel_norm,\n                upmost!.angle_change.abs(),\n                skip = 2,\n            )",
        "narrow_down(\n                upmost!.norm,\n                upmost!.rel_norm,\n                upmost!.angle_change.abs(),\n                skip = 2,\n            )?",
        "narrow_down(\n                upmost!.norm,\n                upmost!.rel_norm,\n                upmost!.angle_change.abs(),\n                skip = 2,\n            )?",
        "narrow_down",
        "long_vertical_dp",
        "long_vertical_dp.norm()",
        "long_vertical_dp",
        "long_vertical_dp.x",
        "long_vertical_dp",
        "long_vertical_dp.y",
        "long_vertical_dp.x / long_vertical_dp.y",
        "2",
        "narrow_down( \n                long_vertical_dp.norm(),\n                long_vertical_dp.x / long_vertical_dp.y,\n                skip = 2,\n            )",
        "narrow_down( \n                long_vertical_dp.norm(),\n                long_vertical_dp.x / long_vertical_dp.y,\n                skip = 2,\n            )?",
        "narrow_down( \n                long_vertical_dp.norm(),\n                long_vertical_dp.x / long_vertical_dp.y,\n                skip = 2,\n            )?",
        "if upmost be Some(_):\n            // ad hoc; improve syntax\n            require upmost!.strokes.end() - upmost!.strokes.start() == 2\n            let long_vertical = upmost!.strokes[upmost!.strokes.start() + 1]\n            let long_vertical_dp = long_vertical.displacement()\n            require long_vertical_dp.y > 0.0\n            narrow_down(\n                upmost!.norm,\n                upmost!.rel_norm,\n                upmost!.angle_change.abs(),\n                skip = 2,\n            )?\n            narrow_down( \n                long_vertical_dp.norm(),\n                long_vertical_dp.x / long_vertical_dp.y,\n                skip = 2,\n            )?",
        "hat",
        "hat be Some(_)",
        "narrow_down",
        "one_fermi_match",
        "one_fermi_match.matches",
        "2",
        "one_fermi_match.matches[2]",
        "one_fermi_match.matches[2]!",
        "one_fermi_match.matches[2]!.norm",
        "one_fermi_match",
        "one_fermi_match.matches",
        "2",
        "one_fermi_match.matches[2]",
        "one_fermi_match.matches[2]!",
        "one_fermi_match.matches[2]!.rel_norm",
        "one_fermi_match",
        "one_fermi_match.matches",
        "2",
        "one_fermi_match.matches[2]",
        "one_fermi_match.matches[2]!",
        "one_fermi_match.matches[2]!.angle_change",
        "one_fermi_match.matches[2]!.angle_change.abs()",
        "2",
        "narrow_down(\n                one_fermi_match.matches[2]!.norm,\n                one_fermi_match.matches[2]!.rel_norm,\n                one_fermi_match.matches[2]!.angle_change.abs(),\n                skip = 2,\n            )",
        "narrow_down(\n                one_fermi_match.matches[2]!.norm,\n                one_fermi_match.matches[2]!.rel_norm,\n                one_fermi_match.matches[2]!.angle_change.abs(),\n                skip = 2,\n            )?",
        "narrow_down(\n                one_fermi_match.matches[2]!.norm,\n                one_fermi_match.matches[2]!.rel_norm,\n                one_fermi_match.matches[2]!.angle_change.abs(),\n                skip = 2,\n            )?",
        "if hat be Some(_):\n            narrow_down(\n                one_fermi_match.matches[2]!.norm,\n                one_fermi_match.matches[2]!.rel_norm,\n                one_fermi_match.matches[2]!.angle_change.abs(),\n                skip = 2,\n            )?",
        "downmost_number_of_strokes",
        "3",
        "downmost_number_of_strokes == 3",
        "downmost_number_of_strokes == 3",
        "require downmost_number_of_strokes == 3",
        "downmost",
        "downmost!",
        "downmost!.strokes",
        "downmost",
        "downmost!",
        "downmost!.strokes",
        "downmost!.strokes.start()",
        "downmost!.strokes[downmost!.strokes.start()]",
        "let downmost_hat = downmost!.strokes[downmost!.strokes.start()]",
        "downmost_hat",
        "downmost_hat.displacement()",
        "let downmost_hat_dp = downmost_hat.displacement()",
        "downmost",
        "downmost!",
        "downmost!.strokes",
        "downmost",
        "downmost!",
        "downmost!.strokes",
        "downmost!.strokes.start()",
        "2",
        "downmost!.strokes.start() + 2",
        "downmost!.strokes[downmost!.strokes.start() + 2]",
        "let downmost_feet = downmost!.strokes[downmost!.strokes.start() + 2]",
        "downmost_feet",
        "downmost_feet.displacement()",
        "let downmost_feet_dp = downmost_feet.displacement()",
        "narrow_down",
        "downmost_hat_dp",
        "downmost_hat_dp.y",
        "downmost_feet_dp",
        "downmost_feet_dp.y",
        "5",
        "narrow_down(\n            downmost_hat_dp.y,\n            downmost_feet_dp.y,\n            skip = 5\n        )",
        "narrow_down(\n            downmost_hat_dp.y,\n            downmost_feet_dp.y,\n            skip = 5\n        )?",
        "narrow_down(\n            downmost_hat_dp.y,\n            downmost_feet_dp.y,\n            skip = 5\n        )?",
        "downmost_number_of_strokes",
        "3",
        "downmost_number_of_strokes == 3",
        "downmost_number_of_strokes == 3",
        "require downmost_number_of_strokes == 3",
        "major_connected_component",
        "major_connected_component.lower_mass",
        "let a = major_connected_component.lower_mass",
        "major_connected_component",
        "major_connected_component.upper_mass",
        "let b = major_connected_component.upper_mass",
        "a",
        "b",
        "a - b",
        "let c = a - b",
        "c",
        "a",
        "c + a",
        "let d = c + a",
        "c",
        "10.0",
        "-10.0",
        "c >= -10.0",
        "c >= -10.0",
        "require c >= -10.0",
        "c",
        "20.0",
        "c <= 20.0",
        "c <= 20.0",
        "require c <= 20.0",
        "OneVsAll::Yes",
        "OneVsAll::Yes",
        "if simp_one_match.norm < 3.0:\n        narrow_down(\n            major_connected_component.max_row_span,\n            skip = 5,\n        )?\n        if major_connected_component.max_row_span > 6.5:\n            require major_connected_component.max_hole_ilen == 0.0\n        OneVsAll::Yes\n    else:\n        require major_connected_component.max_hole_ilen == 0.0\n        require ignored_connected_components_row_span_sum_sum == 0.0\n        let downmost = one_fermi_match.matches[0]\n        let upmost = one_fermi_match.matches[1]\n        let hat = one_fermi_match.matches[2]\n        if downmost be None:\n            require simp_one_match.norm < 4.2\n            narrow_down(\n                simp_one_match.angle_change_norm.abs(),\n                skip = 5,\n            )?\n            require 2.0 * major_connected_component.lower_mass - major_connected_component.upper_mass < 52.0\n            return OneVsAll::Yes\n        let downmost_number_of_strokes = downmost!.strokes.end() - downmost!.strokes.start()\n        require one_fermi_match.norm < 1.0\n        narrow_down(\n            major_connected_component.top_k_row_span_sum(3),\n            one_fermi_match.norm,\n            one_fermi_match.rel_norm,\n            one_fermi_match.angle_change_norm,\n            skip = 12,\n        )?\n        narrow_down(\n            one_fermi_match.matches[0]!.rel_norm,\n            one_fermi_match.matches[0]!.angle_change.abs(),\n            skip = 2,\n        )?\n        if upmost be Some(_):\n            // ad hoc; improve syntax\n            require upmost!.strokes.end() - upmost!.strokes.start() == 2\n            let long_vertical = upmost!.strokes[upmost!.strokes.start() + 1]\n            let long_vertical_dp = long_vertical.displacement()\n            require long_vertical_dp.y > 0.0\n            narrow_down(\n                upmost!.norm,\n                upmost!.rel_norm,\n                upmost!.angle_change.abs(),\n                skip = 2,\n            )?\n            narrow_down( \n                long_vertical_dp.norm(),\n                long_vertical_dp.x / long_vertical_dp.y,\n                skip = 2,\n            )?\n        if hat be Some(_):\n            narrow_down(\n                one_fermi_match.matches[2]!.norm,\n                one_fermi_match.matches[2]!.rel_norm,\n                one_fermi_match.matches[2]!.angle_change.abs(),\n                skip = 2,\n            )?\n        // ad hoc\n        require downmost_number_of_strokes == 3\n        let downmost_hat = downmost!.strokes[downmost!.strokes.start()]\n        let downmost_hat_dp = downmost_hat.displacement()\n        let downmost_feet = downmost!.strokes[downmost!.strokes.start() + 2]\n        let downmost_feet_dp = downmost_feet.displacement()\n        narrow_down(\n            downmost_hat_dp.y,\n            downmost_feet_dp.y,\n            skip = 5\n        )?\n        require downmost_number_of_strokes == 3\n        let a = major_connected_component.lower_mass\n        let b = major_connected_component.upper_mass\n        let c = a - b\n        let d = c + a\n        require c >= -10.0\n        require c <= 20.0\n        OneVsAll::Yes",
        "narrow_down(\n        major_connected_component.max_hole_ilen,\n        ignored_connected_components_row_span_sum_sum,\n        skip = 5,\n    )?\n    let simp_one_match = fermi_match(major_concave_components, [])\n    if simp_one_match.norm < 3.0:\n        narrow_down(\n            major_connected_component.max_row_span,\n            skip = 5,\n        )?\n        if major_connected_component.max_row_span > 6.5:\n            require major_connected_component.max_hole_ilen == 0.0\n        OneVsAll::Yes\n    else:\n        require major_connected_component.max_hole_ilen == 0.0\n        require ignored_connected_components_row_span_sum_sum == 0.0\n        let downmost = one_fermi_match.matches[0]\n        let upmost = one_fermi_match.matches[1]\n        let hat = one_fermi_match.matches[2]\n        if downmost be None:\n            require simp_one_match.norm < 4.2\n            narrow_down(\n                simp_one_match.angle_change_norm.abs(),\n                skip = 5,\n            )?\n            require 2.0 * major_connected_component.lower_mass - major_connected_component.upper_mass < 52.0\n            return OneVsAll::Yes\n        let downmost_number_of_strokes = downmost!.strokes.end() - downmost!.strokes.start()\n        require one_fermi_match.norm < 1.0\n        narrow_down(\n            major_connected_component.top_k_row_span_sum(3),\n            one_fermi_match.norm,\n            one_fermi_match.rel_norm,\n            one_fermi_match.angle_change_norm,\n            skip = 12,\n        )?\n        narrow_down(\n            one_fermi_match.matches[0]!.rel_norm,\n            one_fermi_match.matches[0]!.angle_change.abs(),\n            skip = 2,\n        )?\n        if upmost be Some(_):\n            // ad hoc; improve syntax\n            require upmost!.strokes.end() - upmost!.strokes.start() == 2\n            let long_vertical = upmost!.strokes[upmost!.strokes.start() + 1]\n            let long_vertical_dp = long_vertical.displacement()\n            require long_vertical_dp.y > 0.0\n            narrow_down(\n                upmost!.norm,\n                upmost!.rel_norm,\n                upmost!.angle_change.abs(),\n                skip = 2,\n            )?\n            narrow_down( \n                long_vertical_dp.norm(),\n                long_vertical_dp.x / long_vertical_dp.y,\n                skip = 2,\n            )?\n        if hat be Some(_):\n            narrow_down(\n                one_fermi_match.matches[2]!.norm,\n                one_fermi_match.matches[2]!.rel_norm,\n                one_fermi_match.matches[2]!.angle_change.abs(),\n                skip = 2,\n            )?\n        // ad hoc\n        require downmost_number_of_strokes == 3\n        let downmost_hat = downmost!.strokes[downmost!.strokes.start()]\n        let downmost_hat_dp = downmost_hat.displacement()\n        let downmost_feet = downmost!.strokes[downmost!.strokes.start() + 2]\n        let downmost_feet_dp = downmost_feet.displacement()\n        narrow_down(\n            downmost_hat_dp.y,\n            downmost_feet_dp.y,\n            skip = 5\n        )?\n        require downmost_number_of_strokes == 3\n        let a = major_connected_component.lower_mass\n        let b = major_connected_component.upper_mass\n        let c = a - b\n        let d = c + a\n        require c >= -10.0\n        require c <= 20.0\n        OneVsAll::Yes",
    ],
)
```

## `upmost` decl

```rust
Some(
    [
        "ConcaveComponent",
        "~ConcaveComponent",
        "f32",
        "?f32",
    ],
)
```

## `upmost` defn

```rust
Some(
    [
        "cc",
        "cc.displacement()",
        "let dp = cc.displacement()",
        "dp",
        "dp.y",
        "0.0",
        "dp.y > 0.0",
        "dp.y > 0.0",
        "require dp.y > 0.0",
        "dp",
        "dp.y",
        "dp.y",
        "let dp = cc.displacement()\n    require dp.y > 0.0\n    dp.y",
    ],
)
```

## `downmost` decl

```rust
Some(
    [
        "ConcaveComponent",
        "~ConcaveComponent",
        "f32",
        "?f32",
    ],
)
```

## `downmost` defn

```rust
Some(
    [
        "cc",
        "cc.displacement()",
        "let dp = cc.displacement()",
        "dp",
        "dp.y",
        "0.0",
        "dp.y <= 0.0",
        "dp.y <= 0.0",
        "require dp.y <= 0.0",
        "cc",
        "cc.end()",
        "cc.end().y",
        "-cc.end().y",
        "-cc.end().y",
        "let dp = cc.displacement()\n    require dp.y <= 0.0\n    -cc.end().y",
    ],
)
```

## `hat` decl

```rust
Some(
    [
        "ConcaveComponent",
        "~ConcaveComponent",
        "f32",
        "?f32",
    ],
)
```

## `hat` defn

```rust
Some(
    [
        "cc",
        "cc.displacement()",
        "let dp = cc.displacement()",
        "dp",
        "dp.y",
        "0.0",
        "dp.y < 0.0",
        "dp.y < 0.0",
        "require dp.y < 0.0",
        "dp",
        "dp.x",
        "0.0",
        "dp.x < 0.0",
        "dp.x < 0.0",
        "require dp.x < 0.0",
        "dp",
        "dp.y",
        "-dp.y",
        "dp",
        "dp.x",
        "-dp.y-dp.x",
        "-dp.y-dp.x",
        "let dp = cc.displacement()\n    require dp.y < 0.0\n    require dp.x < 0.0\n    -dp.y-dp.x",
    ],
)
```
