```rust
Some(
    [
        "let raw_angle_change = (((outward as i32) - (inward as i32)) as r32).last_bits(2)",
        "match",
        "let raw_angle_change = (((outward as i32) - (inward as i32)) as r32).last_bits(2)\n    match",
    ],
)
```