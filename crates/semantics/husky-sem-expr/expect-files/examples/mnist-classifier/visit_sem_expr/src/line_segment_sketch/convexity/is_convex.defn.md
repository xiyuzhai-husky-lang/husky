```rust
Some(
    [
        "line_segment_sketch",
        "line_segment_sketch.strokes",
        "line_segment_sketch.strokes.ilen()",
        "let L = line_segment_sketch.strokes.ilen()",
        "line_segment_sketch",
        "line_segment_sketch.strokes",
        "index",
        "L",
        "index%L",
        "line_segment_sketch.strokes[index%L]",
        "line_segment_sketch.strokes[index%L].displacement()",
        "let current_displacement = line_segment_sketch.strokes[index%L].displacement()",
        "line_segment_sketch",
        "line_segment_sketch.strokes",
        "index",
        "1",
        "index-1",
        "(index-1)",
        "L",
        "(index-1)%L",
        "line_segment_sketch.strokes[(index-1)%L]",
        "line_segment_sketch.strokes[(index-1)%L].displacement()",
        "let previous_displacement = line_segment_sketch.strokes[(index-1)%L].displacement()",
        "previous_displacement",
        "current_displacement",
        "previous_displacement.rotation_direction_to(current_displacement)",
        "let is_rotation_counterclockwise_result \n            = previous_displacement.rotation_direction_to(current_displacement)",
        "is_rotation_counterclockwise_result",
        "0",
        "is_rotation_counterclockwise_result == 0",
        "is_rotation_counterclockwise_result == 0",
        "999999.0",
        "-999999.0",
        "let mut previous_raw_cross = -999999.0",
        "line_segment_sketch",
        "line_segment_sketch.strokes",
        "index",
        "1",
        "index-1",
        "(index-1)",
        "L",
        "(index-1)%L",
        "line_segment_sketch.strokes[(index-1)%L]",
        "line_segment_sketch.strokes[(index-1)%L].points",
        "let previous_interval = line_segment_sketch.strokes[(index-1)%L].points",
        "previous_interval",
        "previous_interval.start()",
        "previous_interval",
        "previous_interval.end()",
        "i1",
        "line_segment_sketch",
        "line_segment_sketch.contour",
        "previous_interval",
        "previous_interval.start()",
        "i1",
        "line_segment_sketch.contour.displacement(previous_interval.start(), i1)",
        "let displacement = line_segment_sketch.contour.displacement(previous_interval.start(), i1)",
        "previous_raw_cross",
        "previous_raw_cross",
        "current_displacement",
        "displacement",
        "current_displacement.cross(displacement)",
        "previous_raw_cross.max(current_displacement.cross(displacement))",
        "previous_raw_cross = previous_raw_cross.max(current_displacement.cross(displacement))",
        "previous_raw_cross = previous_raw_cross.max(current_displacement.cross(displacement))",
        "for previous_interval.start() <= i1 < previous_interval.end():\n            let displacement = line_segment_sketch.contour.displacement(previous_interval.start(), i1)\n            previous_raw_cross = previous_raw_cross.max(current_displacement.cross(displacement))",
        "999999.0",
        "-999999.0",
        "let mut current_raw_cross = -999999.0",
        "line_segment_sketch",
        "line_segment_sketch.strokes",
        "index",
        "L",
        "index%L",
        "line_segment_sketch.strokes[index%L]",
        "line_segment_sketch.strokes[index%L].points",
        "let current_interval = line_segment_sketch.strokes[index%L].points",
        "current_interval",
        "current_interval.start()",
        "current_interval",
        "current_interval.end()",
        "i2",
        "line_segment_sketch",
        "line_segment_sketch.contour",
        "previous_interval",
        "previous_interval.start()",
        "i2",
        "line_segment_sketch.contour.displacement(previous_interval.start(), i2)",
        "let displacement = line_segment_sketch.contour.displacement(previous_interval.start(), i2)",
        "current_raw_cross",
        "current_raw_cross",
        "current_displacement",
        "displacement",
        "current_displacement.cross(displacement)",
        "current_raw_cross.max(current_displacement.cross(displacement))",
        "current_raw_cross = current_raw_cross.max(current_displacement.cross(displacement))",
        "current_raw_cross = current_raw_cross.max(current_displacement.cross(displacement))",
        "for current_interval.start() <= i2 < current_interval.end():\n            let displacement = line_segment_sketch.contour.displacement(previous_interval.start(), i2)\n            current_raw_cross = current_raw_cross.max(current_displacement.cross(displacement))",
        "current_raw_cross",
        "previous_raw_cross",
        "current_raw_cross < previous_raw_cross",
        "return current_raw_cross < previous_raw_cross",
        "is_rotation_counterclockwise_result",
        "0",
        "is_rotation_counterclockwise_result > 0",
        "return is_rotation_counterclockwise_result > 0",
        "if is_rotation_counterclockwise_result == 0:\n        let mut previous_raw_cross = -999999.0\n        let previous_interval = line_segment_sketch.strokes[(index-1)%L].points\n        for previous_interval.start() <= i1 < previous_interval.end():\n            let displacement = line_segment_sketch.contour.displacement(previous_interval.start(), i1)\n            previous_raw_cross = previous_raw_cross.max(current_displacement.cross(displacement))\n        // current\n        let mut current_raw_cross = -999999.0\n        let current_interval = line_segment_sketch.strokes[index%L].points\n        for current_interval.start() <= i2 < current_interval.end():\n            let displacement = line_segment_sketch.contour.displacement(previous_interval.start(), i2)\n            current_raw_cross = current_raw_cross.max(current_displacement.cross(displacement))\n        return current_raw_cross < previous_raw_cross\n    else:\n        return is_rotation_counterclockwise_result > 0",
        "let L = line_segment_sketch.strokes.ilen()\n    let current_displacement = line_segment_sketch.strokes[index%L].displacement()\n    let previous_displacement = line_segment_sketch.strokes[(index-1)%L].displacement()\n    let is_rotation_counterclockwise_result \n            = previous_displacement.rotation_direction_to(current_displacement)\n    if is_rotation_counterclockwise_result == 0:\n        let mut previous_raw_cross = -999999.0\n        let previous_interval = line_segment_sketch.strokes[(index-1)%L].points\n        for previous_interval.start() <= i1 < previous_interval.end():\n            let displacement = line_segment_sketch.contour.displacement(previous_interval.start(), i1)\n            previous_raw_cross = previous_raw_cross.max(current_displacement.cross(displacement))\n        // current\n        let mut current_raw_cross = -999999.0\n        let current_interval = line_segment_sketch.strokes[index%L].points\n        for current_interval.start() <= i2 < current_interval.end():\n            let displacement = line_segment_sketch.contour.displacement(previous_interval.start(), i2)\n            current_raw_cross = current_raw_cross.max(current_displacement.cross(displacement))\n        return current_raw_cross < previous_raw_cross\n    else:\n        return is_rotation_counterclockwise_result > 0",
    ],
)
```