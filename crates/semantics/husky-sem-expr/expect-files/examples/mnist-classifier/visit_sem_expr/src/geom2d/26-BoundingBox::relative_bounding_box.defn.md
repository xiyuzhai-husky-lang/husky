```rust
Some(
    [
        "RelativeBoundingBox(\n            self.xrange.relative_range(other.xrange),\n            self.yrange.relative_range(other.yrange),\n        )",
        "RelativeBoundingBox(\n            self.xrange.relative_range(other.xrange),\n            self.yrange.relative_range(other.yrange),\n        )",
    ],
)
```