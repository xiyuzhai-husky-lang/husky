```rust
Some(
    [
        "cc",
        "cc.displacement()",
        "let dp = cc.displacement()",
        "dp",
        "dp.y",
        "0.0",
        "dp.y >= 0.0",
        "dp.y >= 0.0",
        "require dp.y >= 0.0",
        "Some",
        "cc",
        "cc.bounding_box",
        "cc.bounding_box.ymin()",
        "-cc.bounding_box.ymin()",
        "Some(-cc.bounding_box.ymin())",
        "Some(-cc.bounding_box.ymin())",
        "let dp = cc.displacement()\n    require dp.y >= 0.0\n    Some(-cc.bounding_box.ymin())",
    ],
)
```