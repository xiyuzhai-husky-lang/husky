```rust
Some(
    [
        "LineSegment(\n            self.strokes.first()!.start.clone(),\n            self.strokes.last()!.end.clone()\n        )",
        "LineSegment(\n            self.strokes.first()!.start.clone(),\n            self.strokes.last()!.end.clone()\n        )",
    ],
)
```