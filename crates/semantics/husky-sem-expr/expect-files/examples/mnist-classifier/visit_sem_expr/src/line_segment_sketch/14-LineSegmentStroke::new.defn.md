```rust
Some(
    [
        "assert from <= to",
        "LineSegmentStroke(ct.points.cyclic_slice_leashed(from, to + 1))",
        "assert from <= to\n        LineSegmentStroke(ct.points.cyclic_slice_leashed(from, to + 1))",
    ],
)
```