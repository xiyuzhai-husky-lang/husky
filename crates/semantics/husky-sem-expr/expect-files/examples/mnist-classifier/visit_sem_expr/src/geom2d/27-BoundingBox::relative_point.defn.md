```rust
Some(
    [
        "RelativePoint2d",
        "self",
        "self.xrange",
        "point",
        "point.x",
        "self.xrange.relative_point(point.x)",
        "self",
        "self.yrange",
        "point",
        "point.x",
        "self.yrange.relative_point(point.x)",
        "RelativePoint2d(\n            self.xrange.relative_point(point.x),\n            self.yrange.relative_point(point.x),\n        )",
        "RelativePoint2d(\n            self.xrange.relative_point(point.x),\n            self.yrange.relative_point(point.x),\n        )",
        "RelativePoint2d(\n            self.xrange.relative_point(point.x),\n            self.yrange.relative_point(point.x),\n        )",
    ],
)
```