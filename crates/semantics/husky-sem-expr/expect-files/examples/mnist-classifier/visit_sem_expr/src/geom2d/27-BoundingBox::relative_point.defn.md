```rust
Some(
    [
        "RelativePoint2d(\n            self.xrange.relative_point(point.x),\n            self.yrange.relative_point(point.x),\n        )",
        "RelativePoint2d(\n            self.xrange.relative_point(point.x),\n            self.yrange.relative_point(point.x),\n        )",
    ],
)
```