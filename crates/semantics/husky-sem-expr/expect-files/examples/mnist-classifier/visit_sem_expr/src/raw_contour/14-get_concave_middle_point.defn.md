```rust
Some(
    [
        "let N = points.ilen()",
        "let p0 = points[N-2]",
        "let p2 = points[N-1]",
        "Point2d(\n        (p0.x + p2.x) / 2.0, \n        (p0.y + p2.y) / 2.0,\n    )",
        "let N = points.ilen()\n    let p0 = points[N-2]\n    let p2 = points[N-1]\n    Point2d(\n        (p0.x + p2.x) / 2.0, \n        (p0.y + p2.y) / 2.0,\n    )",
    ],
)
```