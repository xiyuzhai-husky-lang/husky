```rust
Some(
    [
        "if low < high:\n        let p = partition(arr, low, high)\n        quick_sort_aux(arr, low, p - 1)\n        quick_sort_aux(arr, p + 1, high)",
        "if low < high:\n        let p = partition(arr, low, high)\n        quick_sort_aux(arr, low, p - 1)\n        quick_sort_aux(arr, p + 1, high)",
    ],
)
```