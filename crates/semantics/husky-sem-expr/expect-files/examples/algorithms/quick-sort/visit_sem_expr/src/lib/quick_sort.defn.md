```rust
Some(
    [
        "arr",
        "arr.len()",
        "let len = arr.len()",
        "quick_sort_aux",
        "arr",
        "0",
        "len",
        "1",
        "len - 1",
        "(len - 1)",
        "isize",
        "(len - 1) as isize",
        "quick_sort_aux(arr, 0, (len - 1) as isize)",
        "quick_sort_aux(arr, 0, (len - 1) as isize)",
        "let len = arr.len()\n    quick_sort_aux(arr, 0, (len - 1) as isize)",
    ],
)
```