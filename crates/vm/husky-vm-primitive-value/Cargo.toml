[package]
name = "husky-vm-primitive-value"
version = "0.0.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.106", features = ["derive"], optional = true }

[features]
serde_support = ["dep:serde"]
