[package]
edition = "2021"
name = "infer-total"
version = "0.0.0"
authors = ["Xiyu Zhai"]
license = "MIT OR Apache-2.0"
description = "Generic LSP server scaffold."

[dependencies]
check-utils = { path = "../../utils/check", version = "0.0.0" }
dev-utils = { path = "../../utils/dev", version = "0.0.0" }
print-utils = { path = "../../utils/print", version = "0.0.0" }
infer-error = { path = "../error", version = "0.0.0" }
decl = { path = "../../syntax/decl", version = "0.0.0" }
infer-ty = { path = "../ty", version = "0.0.0" }
infer-contract = { path = "../contract", version = "0.0.0" }
