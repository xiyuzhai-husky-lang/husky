use ast::RawExprArena;

use super::*;

impl<'a> QualifiedTySheetBuilder<'a> {
    pub(super) fn infer_morphism(
        &mut self,
        arena: &RawExprArena,
        ast_iter: AstIter,
        opt_output_ty: Option<EntityRoutePtr>,
    ) {
        todo!()
    }
}
