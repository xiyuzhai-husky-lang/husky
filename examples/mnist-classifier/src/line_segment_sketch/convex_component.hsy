use crate::line_segment_sketch::*

struct ConvexComponent {
    line_segment_sketch: &LineSegmentSketch,
    line_segments: [%]LineSegmentStroke,
}

impl Visualize for ConvexComponent:
    fn visualize() -> Html:
        self.line_segments.visualize()