func add(x: i32, y: i32) -> i32:
    x + y

dataset:
    synthetic::trivial::real1d::dataset1()

main:
    assert add(1,2) == 3
    assert 1 - 2 == -1
    assert 3 * 2 == 6
    assert 3 / 2 == 1
    assert 3 ** 2 == 9