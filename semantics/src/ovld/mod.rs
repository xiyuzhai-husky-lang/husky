mod ovld_matcher;
// mod parse_ovld_matchers;

use super::*;
use common::*;

pub struct Ovld {
    pub inputs: Vec<ScopeID>,
    pub output: Scope,
}
pub struct OvldID {}
// pub use parse_ovld_matchers::*;
