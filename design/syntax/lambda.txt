Plan 1:
```
|x: i32| -> i32 = {
    x + 1
}
```

|x: i32| -> i32 :{
    x + 1
}

|x: i32| -> i32 = {
    x + 1
}

|x: i32| -> i32 :{
    x + 1
}:

|x: i32| -> i32 {:
    x + 1
:}

|x: i32| -> i32 {:
    x + 1
:}